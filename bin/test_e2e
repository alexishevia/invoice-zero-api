#!/bin/bash

set -e # exit if any command fails

CURRENT_FILE=`realpath $0`
CURRENT_DIR=`dirname $CURRENT_FILE`

# read env file
. $CURRENT_DIR/../.env

# run migrations
docker-compose run dbmigrate up -e e2e

# run e2e tests
docker-compose run e2e_runner npx mocha --exit "tests/**/*.test.mjs"
