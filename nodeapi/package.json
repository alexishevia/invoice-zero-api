{
  "name": "invoice-zero-api",
  "version": "0.0.5",
  "private": true,
  "scripts": {
    "prestart": "touch /usr/src/data/persistence.mdjson",
    "start": "nodemon --inspect=0.0.0.0 ./bin/www.mjs",
    "test": "mocha tests/**/*.test.mjs",
    "test:debug": "mocha --inspect-brk=0.0.0.0:$TEST_INSPECTOR_PORT tests/**/*.test.mjs",
    "test:single": "mocha",
    "test:single:debug": "mocha --inspect-brk=0.0.0.0:$TEST_INSPECTOR_PORT"
  },
  "dependencies": {
    "basic-auth": "^2.0.1",
    "cors": "^2.8.5",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "split2": "^3.2.2",
    "uuid": "^8.3.2"
  },
  "devDependencies": {
    "chai": "^4.3.4",
    "mocha": "^8.4.0",
    "nodemon": "^2.0.7",
    "supertest": "^6.1.3"
  }
}
